import Example from '../components/Example';
import * as Styled from './styles';

const Home = () => {
  return (
    <Styled.Wrapper>
      <Example />
    </Styled.Wrapper>
  );
};

export default Home;
